# Email config (choose one approach)
# For Nodemailer + SMTP:
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="AZ Media <no-reply@az-media.ca>"

# Optional: Resend or other providers could also be used instead.
# RESEND_API_KEY=
# RESEND_FROM=AZ Media <no-reply@az-media.ca>
